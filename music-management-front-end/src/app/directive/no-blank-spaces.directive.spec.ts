import { NoBlankSpacesDirective } from './no-blank-spaces.directive';

describe('NoBlankSpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new NoBlankSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
